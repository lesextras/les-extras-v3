# --- Base de données ---
DATABASE_URL="postgresql://lesextras:lesextras@localhost:5432/lesextras?schema=public"
# --- Auth ---
JWT_SECRET="change-me-in-prod"
SESSION_SECRET="change-me-in-prod"
JWT_EXPIRES_IN="7d"
# --- API ---
API_PORT=3001
API_BASE_URL="http://localhost:3001/api"
# --- Web ---
NEXT_PUBLIC_API_URL="http://localhost:3001/api"
WEB_PORT=3000
# --- Mail (invitations, notifications) ---
MAIL_DSN="smtp://user:pass@smtp.hostinger.com:587"
MAIL_FROM="LES EXTRAS <no-reply@les-extras.fr>"
# --- Uploads ---
UPLOAD_DIR="./uploads"
# --- CORS ---
# Origines autorisées, séparées par des virgules. Laissé vide, l'API n'accepte
# que le site de production (et localhost:3000 hors production) : jamais
# « toutes origines », qui, combiné aux cookies de session, laisserait
# n'importe quel site appeler l'API au nom d'un utilisateur connecté.
CORS_ORIGINS=""
# --- Facturation ---
# Compte de l'association ADéPA. Seules SES factures s'encaissent en ligne :
# encaisser celle d'un intervenant serait recevoir des fonds pour le compte
# d'un tiers (art. L. 521-2 et L. 522-1 du code monétaire et financier).
# Laissé vide, le compte est reconnu par son nom (« ADéPA »).
PLATFORM_ACCOUNT_ID=""
# --- Mesure des campagnes publicitaires (facultatif) ---
# Tant que NEXT_PUBLIC_GOOGLE_ADS_ID est vide, AUCUN traceur n'est chargé :
# le bandeau cookies reste une simple information et la page /legal/cookies
# affiche « aucun traceur publicitaire ». Dès que l'identifiant est posé, le
# bandeau devient un vrai gestionnaire de consentement (Accepter / Refuser,
# refus par défaut, choix révocable) et le tag n'est chargé qu'après un oui.
# Les deux valeurs se lisent dans Google Ads → Objectifs → Conversions.
NEXT_PUBLIC_GOOGLE_ADS_ID=""
NEXT_PUBLIC_GOOGLE_ADS_CONVERSION_INSCRIPTION=""

# --- Assistant LEX (rédaction et générateur d'activités) ---
# Sans aucune de ces deux clés, LEX se tait : il ne plante pas, mais il ne
# produit plus rien. C'est le premier réglage à poser sur un nouveau serveur.
# Claude travaille dès que sa clé est présente ; Mistral prend le relais sinon.
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-sonnet-4-20250514"
MISTRAL_API_KEY=""
MISTRAL_MODEL="mistral-large-latest"

# --- Paiement en ligne (packs de crédits) ---
# Sans ces clés, l'achat de crédits est refusé proprement et le reste de la
# plateforme continue de fonctionner.
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
# Adresse publique du site, utilisée pour les retours de paiement.
APP_WEB_URL="http://localhost:3000"

# --- Adresses de service ---
# Destinataire des alertes internes (candidature, signalement).
MAIL_ADMIN="assoc.adepa@gmail.com"
# Expéditeur technique, quand il doit différer de MAIL_FROM.
MAIL_FROM_ADDRESS="no-reply@les-extras.fr"

# --- Objectif affiché sur le tableau de bord de l'association ---
OBJECTIF_CAMPAGNE="10000"
OBJECTIF_ECHEANCE="2027-12-31"
